export enum ServiceType {
  category = 'category',
  categoryItem = 'categoryItem',
  categoryLevel = 'categoryLevel',
  node = 'node',
  nodeDef = 'nodeDef',
  record = 'record',
  survey = 'survey',
  taxon = 'taxon',
  taxonomy = 'taxonomy',
  user = 'user',
}
